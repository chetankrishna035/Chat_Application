{
  "name": "smart-chat-app-monorepo",
  "version": "1.0.0",
  "description": "Full-stack real-time chat app",
  "scripts": {
    "start": "node server/server.js",
    "install-server": "cd server && npm install",
    "build": "npm run install-server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
